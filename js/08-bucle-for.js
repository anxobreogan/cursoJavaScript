'use strict';
// Bucle for.

//Def.- Trozo de codigo que se ejecuta un número de veces.

//for(let i=0 (contador); i<100 (condición);i++ (el paso))

//Con debugger lo que hacemos es parar el bucle.
var numero= 100;

for(let i=0; i<numero;i++){
	console.log(i);

	//debugger;
}